server:
  port: 9005
spring:
  application:
    name: ten-square-artcle
  cache:
    type: redis
    ehcache:
      config: classpath:ehcache.xml
spring:
  profiles:
    active: dev


# Logger Config
logging:
  level:
    com.baomidou.mybatisplus.samples.deluxe: debug

