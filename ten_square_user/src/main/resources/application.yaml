server:
  port: 9009
spring:
  application:
    name: ten-square-user
  cache:
    type: redis
    ehcache:
      config: classpath:ehcache.xml
spring:
  profiles:
    active: dev


# Logger Config
logging:
  level:
    com.baomidou.mybatisplus.samples.deluxe: debug

