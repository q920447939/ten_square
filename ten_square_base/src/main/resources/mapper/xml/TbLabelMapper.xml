<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.withmes.base.mapper.TbLabelMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="cn.withmes.base.entity.TbLabel">
                    <id column="id" property="id" />
                    <result column="labelname" property="labelname" />
                    <result column="state" property="state" />
                    <result column="count" property="count" />
                    <result column="recommend" property="recommend" />
                    <result column="fans" property="fans" />
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="Base_Column_List">
        id, labelname, state, count, recommend, fans
    </sql>

    <select id="demo" resultType="java.lang.Integer">
        select 1 from dual
    </select>
</mapper>